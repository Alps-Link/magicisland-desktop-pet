# 变更记录

每个发行版一个标签；成品 zip 的体积与 SHA256 见 `BUILD_MANIFEST.md`。

## v2026.09.13

**主题：点击动画 → 待机 的"接缝"问题收口（8 只同步）**

### 修复
- **接缝处不再做任何事**：删掉旧的"收尾回归"整段（全参数 lerp + 重启待机）与后期试过的"角度预存""`Drag(0,0)` 清偏移""0.3s drag 渐入"。点击/唤醒只保留 `StopAllMotions()` + `Touch(3)`。
  - 呼吸不再在动作结束那一帧被顶起（实测曾 +0.33~0.64）；
  - 残姿由 drag 按绝对位置继续叠加，不再出现"回正 → 再回 drag 位"的抽搐。
- **嘴的归属改成三段式**：①表情挂着 → 完全交给表情；②表情回归过渡中且没在说话 → 交给过渡（平滑收嘴）；③其余（含过渡中正在说话）→ 由渲染循环每帧写（收回动作末帧残留 + 跟 App 说话值）。
- 修掉两处上轮引入的回归：薇薇/艾丽卡"回归待机时嘴一帧闭死"（0.500/0.496 → 0.012）、阿尔卑斯等"表情张嘴 0.5s 后被抹掉"（0.251 → 0.003）。
- 删除 4 只（可可/洛洛/莫娜卡/卡梅莉亚）里**死掉的重复 `wake`/`click` 分支**——活着的那份 `wake` 是旧版，导致"唤醒后脸不回中性"。
- 清掉临时取证代码、死变量、以及 7 只共有的过期注释。

### 实测（离屏台子，接缝窗口最大单帧跳变）
阿尔卑斯 0.012 / 希雅拉 0.010 / 可可 0.007 / 莫娜卡 0.044 / 卡梅莉亚 0.058；
薇薇 0.497、洛洛 0.490、艾丽卡 0.232 逐帧确认为**模型自带动作的平滑位移**，非跳变。

### 已知未处理（记录在案）
- **换表情时头部会跳**：真因＝表情过渡会写 `Angle*`（场景文件里带 `AngleX` 等姿态值），与每帧叠加的 drag 冲撞。
  两条无效修法（已实测）：去掉 drag 守卫、表情期间 drag 全程失效。有效但带取舍的修法：过渡不写角度（代价：表情不再带动头部姿态）。
  触发条件＝光标离线时切换表情；打包版同款，属历史行为。
- 无表情回复的说话口型仍按 App 的固定 2 秒开合（用户已确认维持）；带表情回复的嘴随表情、气泡淡出时回中性。
- 中性回复、睡眠/醒来经审计**无问题**（睡眠期间 App 会把鼠标值归零，"头回正"是设计行为）。
